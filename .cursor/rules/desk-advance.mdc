---
description: Desk 推进必须先讲效果再给文件；按生命周期缺口，禁止只丢代码
alwaysApply: true
---

# Desk 推进方式

用户在学控制面，不是赶进度。默认 **explore/教学**，除非用户明确说「代写/直接改仓库」。

课表不定 21 天。**一块课 = 一次 Run 上还没接通的边**，一次写到单环要用的深度。不要按插件清单横切，不要先 stub 再撕。

**每期推进开头必须标：** `单环 x/8 · 成品 y/6 · 观察 z/2`（分母见 `document/runlog/plan.md`）。观察格在单环 8/8 之后才动。观察 A/B 的 React+TS dashboard **助手代写、用户 review**；不要改成让用户手写前端。

## 每次推进（含「推进」、补课）必须按这个顺序，缺一块就停

1. **今天做成什么效果**：用户能敲哪条命令、看到什么响应、库里多什么。对照「现在还没有什么」。
2. **一次请求怎么走**（5–8 步）：入口 → 哪个函数 → 哪张表。不要先贴整文件。
3. **分文件**：每个将改/将建的文件各一段。必须写清：
 - **局部**：这个包自己负责什么、对外几个函数
 - **全局**：它在「curl → Gin → store → Postgres」里接哪一段；不接的不要假装接了
4. **再给代码**。用户手写业务；过关标准可执行（优先 curl / 库里能查到什么）。

## 测试谁写

- **用户手写：** 过关等于一条不变量的那天——事务回滚、状态机非法转移、jail/超时、工具上限。测例就是课。
- **可省略或助手代写：** HTTP 组装、healthz、SSE 长连接、为了 `go test` 变绿的回归。用户用 curl 过关即可，不准把「让助手写测试」当成推进日常。
- 一天最多要求用户写 **1** 个不变量测试；没有不变量就不布置 `*_test.go`。

禁止：只给文件清单；把成品全图当「今天已实现」；同一课塞进下一条边（Ask 课不做 cancel/CLI）。

## 用户说跟不上时

停写新功能。先用现有真实代码做请求追踪，直到用户能自己口述一条 `POST .../messages` 的路径。
