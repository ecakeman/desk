---
description: Desk 每日推进必须先讲效果再给文件；禁止只丢代码
alwaysApply: true
---

# Desk 推进方式

用户在学控制面，不是赶进度。默认 **explore/教学**，除非用户明确说「代写/直接改仓库」。

## 每次推进（含「推进」、D4+、补课）必须按这个顺序，缺一块就停

1. **今天做成什么效果**：用户能敲哪条命令、看到什么响应、库里多什么。对照「现在还没有什么」（Python / 插件 / SSE / CLI 未做就写明）。
2. **一次请求怎么走**（5–8 步）：入口 → 哪个函数 → 哪张表。不要先贴整文件。
3. **分文件**：每个将改/将建的文件各一段。必须写清：
   - **局部**：这个包自己负责什么、对外几个函数
   - **全局**：它在「curl → Gin → store → Postgres」里接哪一段；不接的不要假装接了
4. **再给代码**。用户手写业务；过关标准可执行（优先 curl / 库里能查到什么）。

## 测试谁写

- **用户手写：** 过关等于一条不变量的那天——事务回滚、状态机非法转移、jail/超时、工具上限。测例就是课。
- **可省略或助手代写：** HTTP 组装、healthz、SSE 长连接、为了 `go test` 变绿的回归。用户用 curl 过关即可，不准把「让助手写测试」当成推进日常。
- 一天最多要求用户写 **1** 个不变量测试；没有不变量就不布置 `*_test.go`。

禁止：只给文件清单；把成品全图当「今天已实现」；同一天塞进下一天的口。

## 用户说跟不上时

停写新功能。先用现有 D 的真实代码做请求追踪，直到用户能自己口述一条 `POST .../messages` 的路径。
