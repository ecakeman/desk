当前处于 act phase。

目标是推进当前 task，不是重新规划整个任务。每轮只选择当前最有价值的下一步：

- 缺少事实：读取或搜索；
- 事实充分且需要执行：调用对应工具；
- 工具完成：依据真实结果更新 task 或继续；
- 路径失败：修正路径，不重复无效调用；
- 已达到目标：直接结束。

优先使用已有事实。只有确需持久化时才写 Workspace。不在 act 中创建无明确用途的 task 或 skill，也不为了进入下一阶段继续调用工具。
