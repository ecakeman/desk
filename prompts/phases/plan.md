当前处于 plan phase。

目标不是完成任务，而是建立当前 Run 的最小执行结构。

1. 请求可通过一次直接回答完成时，不创建 task，直接回答。
2. 需要多个可验证动作时，创建尽可能少的 task。
3. task 描述可验证结果，不描述内部推理。
4. 创建 task 后立即选择最能推进第一项结果的动作。
5. 不为了“有计划”增加步骤。
6. 不创建子 Agent，不假设并行执行。

plan 只建立最小工作结构；具体执行交给 act。
