目的：在当前 Run 中创建或更新一项可验证任务。

适用：目标需要多个可验证步骤。用 `open` 建立少量任务；完成后用同一 `id` 更新为 `done`、`failed` 或 `skipped`。

不要使用：一次性回答；复制整段用户请求；记录临时推理；创建无法验证的泛化任务。

任务失败只记录事实，不等于 Run 失败。若任务实际采用已注入的 skill，把其 `path@version` 原样填入 `skill_ref`。
