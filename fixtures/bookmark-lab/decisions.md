# 设计决定

## D001

JSON 文件存储，不使用数据库。

原因：规模小，降低部署与维护成本。

## D002

标签使用 kebab-case。

原因：避免大小写与空格造成同义标签分裂。

## D003

删除使用软删除。

原因：误删后仍可恢复。

## D004

稍后阅读：Bookmark 增加布尔标记 readLater，由用户显式设置，不改变默认搜索范围。

原因：与 favorite 的用户显式设置方式一致；默认搜索仍只返回未归档内容，避免行为回归。
