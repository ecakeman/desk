{
    "name": "search",
    "command": "search",
    "risk": "read",
    "ops": [
      {
        "name": "grep",
        "description": "Search workspace files for a substring.",
        "parameters": {
          "type": "object",
          "additionalProperties": false,
          "required": ["query"],
          "properties": {
            "query": {"type": "string"},
            "glob": {"type": "string"}
          }
        }
      }
    ]
  }